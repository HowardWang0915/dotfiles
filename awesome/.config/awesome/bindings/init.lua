return {
    client = {
        key = require("bindings.client.key"),
        mouse = require("bindings.client.key")
    },
    global = {
        key = require("bindings.global.key"),
        mouse = require("bindings.global.key")
    },
    -- this file defines which key shall be used for mods
    mod = require("bindings.mod")
}
